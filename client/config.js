// Leave blank when this frontend is served by the Express server.
// If Render hosts the frontend and backend as separate services, set this to
// the backend service URL, for example: "https://your-api-name.onrender.com".
window.PATIENT_API_BASE_URL = "";
